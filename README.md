# cicd-pipeline-tracker
A Next.js dashboard tracking GitHub CI/CD builds, test runs, and multi-environment deployments in real time via secure webhooks (HMAC verified). Stores run history in PostgreSQL with Prisma and surfaces live status badges, commit logs, and failure details.
